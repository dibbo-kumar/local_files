#include <iostream>
#include <stack>

using namespace std;

int main()
{
    // stack<int> s;
    // s.push(8);
    // s.push(9);
    // cout << "element is " << s.top() << endl;
    // //s.pop();
    // cout << "element is " << s.top() << endl;
    // cout<< "size is "<< s.size() << endl;
    class stack
    {
        
    };

    return 0;
}
