choosing right sorting algorithm can improve performance effectively
for small data range such as serial number between 1 to 50 , here 
counting sort will work very fast ,  because it has highest size of 50.
if there was huge number such as 6 or 7 digit , then counting sort will take 
huge time, here radix sort will work very fast. and
when the data is a mix of decimals, strings
values, Merge Sort is  the fastest. so i can say that Counting Sort finishes
tiny ranges almost instantly, Radix handles big numbers efficiently,
and Merge Sort gives consistent results across any kind of messy 
data.